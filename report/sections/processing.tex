\clearpage
\section*{Text Processing}
\phantomsection
\addcontentsline{toc}{section}{Text Processing}

% ============================================================================= 
% NORMALIZATION
% ============================================================================= 
\subsection*{Surface Level Normalization Definition}
\phantomsection
\addcontentsline{toc}{subsection}{Surface Level Normalization Definition}

Before tokenizing and any further text processing, the text undergoes a 
three-stage cleaning process to ensure variations in formatting do not create 
duplicate tokens in our index. 
\begin{enumerate}
    \item   The raw text is cast to lowercase with the \texttt{.lower()} 
            function for strict case-insensitivity.
    \item   We strip out any punctuation, including periods, commas, quotes, 
            brackets, and special symbols using a regular expression. This was 
            applied with 
            \texttt{re.sub(r'[\textasciicircum \textbackslash w\textbackslash s]',
            '')}, which identifies any character that is $not$ a word 
            character (numbers included) or a whitespace character, and replaces
            it with nothing. 
    \item   We again use a regular expression to target contiguous sequences  
            of whitespaces, collapsing them to a single, clean space. 
            Additionally, we apply the \texttt{.strip()} function to this 
            regular expression to trim any lingering spaces at the beginning 
            and end of a document. 
\end{enumerate}


% =============================================================================
% TOKENIZER
% =============================================================================
\subsection*{Tokenizer Definition}
\phantomsection
\addcontentsline{toc}{subsection}{Tokenizer Definition}

The now surface-level normalized text is segmented into discrete vocabulary 
tokens by calling the \texttt{.split()} function along the whitespace 
boundaries. 

% ============================================================================= 
% STOPPING
% ============================================================================= 
\subsection*{Stopping Definition}
\phantomsection
\addcontentsline{toc}{subsection}{Stopping Definition}

Simultaneously alongside the tokenization, each token is 
immediately checked against the \texttt{ENGLISH\_STOP\_WORDS}
list provided by the \texttt{scikit-learn} library. If the token is a highly 
common word, (i.e., "the", "is", "at", etc.), the token is instantly discarded. 
This results in a final tokens list full of meaningful words. 

% ============================================================================= 
% STEMMING/LEMMATIZATION
% ============================================================================= 
\subsection*{Stemming/Lemmatizing Definition}
\phantomsection
\addcontentsline{toc}{subsection}{Stemming/Lemmatizing Definition}

The remaining list of tokens is then looped over by \texttt{nltk}'s 
\texttt{PorterStemmer()} for algorithmic suffix-stripping. This algorithm 
removes common English suffixes to ensure that our vocabulary word variants 
converge to a single root index term. 

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{../eval/figures/txt_processing.png}
    \caption[4-Stage Text Processing Pipeline]{
        An example string passed through the four-stage text processing pipeline. 
        Raw text is passed through surface-level normalization, tokenization, 
        stop-word removal, and stemming before use in the system's index.
    }
    \label{figure:text_processing_pipeline}
\end{figure}